# 🌐 Backend API URL
REACT_APP_API_URL=http://localhost:7000

# 📌 Notes:
# This variable is used in the React app to connect to the backend API.
# React requires all environment variables to be prefixed with REACT_APP_
# You can change the value to match your deployed backend URL in production.
